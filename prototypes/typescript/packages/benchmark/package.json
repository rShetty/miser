{
  "name": "@miser/benchmark",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "bin": "src/bin.ts",
  "scripts": {
    "start": "bun run src/bin.ts",
    "build": "bun build src/bin.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@miser/config": "workspace:*",
    "@miser/classifier": "workspace:*",
    "@miser/router": "workspace:*"
  }
}
